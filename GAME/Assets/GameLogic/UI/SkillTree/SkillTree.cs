using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SkillTree : MonoBehaviour
{
    public GameObject Tree;
    void Start()
    {
      //  Tree.SetActive(false);
    }

  
    void Update()
    {
      //  if (Sunset.dayCount != 0 && Sunset.dayCount % 2 == 0 && Sunset.day == false)
            //Tree.SetActive(true);
    }
}
